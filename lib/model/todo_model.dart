class TodoModel {
  String title;
  String content;
  String workTime;
  String name;
  String completeDate;
  String lateDate;
  bool isComplete;

  TodoModel(this.title, this.content, this.workTime, this.name,
      this.completeDate, this.lateDate, this.isComplete);
}
